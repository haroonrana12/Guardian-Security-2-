// The shared Start config bundles TanStack Start, React, Tailwind, path aliases, and related
// plugins — avoid re-adding those here or you may get duplicate registrations.
// Extend via defineConfig({ vite: { ... } }) when needed.
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig();
